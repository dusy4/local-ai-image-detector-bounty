{
  "name": "local-lens-ai-detector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "model:download": "node scripts/download-model.mjs",
    "calibration:prepare": "node scripts/prepare-owned-calibration-set.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "check": "npm run test && npm run build && node --check dist/offscreen.js && node --check dist/content-script.js",
    "benchmark": "node scripts/run-browser-benchmark.mjs"
  },
  "devDependencies": {
    "puppeteer-core": "^25.6.0"
  }
}
